# Copyright (c) 2023 Mike Tsao. All rights reserved.

[package]
default-run = "ensnare-daw"
description = "Audio tools"
documentation = "https://docs.rs/ensnare/"
edition = "2021"
homepage = "https://ensnare.rs"
license = "MIT"
name = "ensnare"
readme = "README.md"
repository = "https://github.com/ensnare-org/ensnare"
version = "0.0.2"

[dependencies]
anyhow = "1.0.75"
app_dirs2 = "2.5"
atomic-counter = "1.0.1"
bit-vec = "0.6.3"
btreemultimap = "0.1.1"
cpal = "0.15.2"
crossbeam = "0.8.2"
crossbeam-channel = "0.5.8"
derive_builder = "0.12.0"
derive_more = "0.99.17"
eframe = "0.22"
egui_extras = { version = "0.22" }
ensnare-core = { path = "core" }
ensnare-proc-macros = { path = "proc-macros" }
float-cmp = "0.9.0"
hound = "3.5.0"
kahan = "0.1.4"
midir = "0.9"
midly = "0.5.3"
more-asserts = "0.3.1"
nalgebra = "0.32.3"
once_cell = "1.18.0"
oorandom = "11.1.3"
rayon = "1.8.0"
serde = { version = "1.0", features = ["derive", "rc"] }
serde_json = "1.0"
spectrum-analyzer = "1.5.0"
strum = "0.25.0"
strum_macros = "0.25.2"
typetag = "0.2.13"

[workspace]
members = ["core", "proc-macros"]

[dev-dependencies]
clap = { version = "4.4.4", features = ["derive"] }
crossbeam-channel = "0.5.8"
egui-toast = { version = "0.8" }
regex = "1.7"
env_logger = "0.10"
serde_json = "1.0"

[[bin]]
name = "ensnare-daw"

[lib]
name = "ensnare"
path = "src/lib.rs"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"

[package.metadata.cross.target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt -qq update",
    "apt -y install pkg-config libfontconfig-dev:$CROSS_DEB_ARCH libasound2-dev:$CROSS_DEB_ARCH"
]
# See https://github.com/iced-rs/iced/blob/master/Cross.toml
image = "ghcr.io/iced-rs/aarch64:latest"
xargo = false

[package.metadata.cross.target.armv7-unknown-linux-gnueabihf]
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt -qq update",
    "apt -y install pkg-config libfontconfig-dev:$CROSS_DEB_ARCH libasound2-dev:$CROSS_DEB_ARCH"
]
# See https://github.com/iced-rs/iced/blob/master/Cross.toml
image = "ghcr.io/iced-rs/armv7:latest"
xargo = false

[package.metadata.deb]
assets = [
    # "You can use target/release/ in asset paths, even if Cargo is configured
    # to cross-compile or use custom CARGO_TARGET_DIR. The target dir paths will
    # be automatically corrected." https://github.com/kornelski/cargo-deb
    ["LICENSE.txt", "usr/share/doc/ensnare/LICENSE.txt", "644"],
    ["README.md", "usr/share/doc/ensnare/README.md", "644"],
    ["assets-nodist/os/ensnare.desktop", "usr/share/applications/", "644"],
    ["assets/patches/**/*.yaml", "usr/share/ensnare/patches/", "644"],
    ["assets/samples/**/*.wav", "usr/share/ensnare/samples/", "644"],
    ["projects/**/*.yaml", "usr/share/ensnare/projects/", "644"],
    ["target/release/ensnare-daw", "usr/bin/", "755"],
]
